import { FilterEmployeesByServicePipePipe } from './filter-employees-by-service.pipe.pipe';

describe('FilterEmployeesByServicePipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterEmployeesByServicePipePipe();
    expect(pipe).toBeTruthy();
  });
});
